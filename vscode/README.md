# User settings for VS Code

`settings.json` and `keybindings.json` live in `~/Library/Application Support/Code/User` on a Mac.
The safest is to copy them there and maintain the files in this repo by comparing diffs.
